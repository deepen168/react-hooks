import photos from './data/pictures';

export const Gallary = () => {

    const numberOfPhotos = photos.length;

    return (
        <div>
            {
                
            }
        </div>
    )
}